#include PARENT( StGermain/Base/Foundation/NewClass )

#undef CLASSNAME
#define CLASSNAME MeshTopology

VOIDOVERRIDE( Construct, void, (void* self), (self) )
VOIDOVERRIDE( Destruct, void, (void* self), (self) )
VOIDOVERRIDE( Copy, void, (void* self, const void* op), (self, op) )
OVERRIDE( CalcMem, SizeT, (const void* self, PtrMap* ptrs), (self, ptrs) )

METHOD( SetNumDims, void, (void* self, int nDims), (self, nDims) )
METHOD( SetComm, void, (void* self, const Comm* comm), (self, comm) )
METHOD( SetDomain, void, (void* self, int dim, Sync* sync), (self, dim, comm) )
METHOD( SetElements, void, (void* self, int dim, int nEls, const int* globals), 
	(self, dim, nEls, globals) )
METHOD( SetLocalElements, void, (void* self, int dim, int nEls, 
				 const int* globals), 
	(self, dim, nEls, globals) )
METHOD( AddLocalElements, void, (void* self, int dim, int nEls, 
				 const int* globals), 
	(self, dim, nEls, globals) )
METHOD( RemoveLocalElements, void, (void* self, int dim, int nEls, 
				    const int* globals, IMap* map), 
	(self, dim, nEls, globals, map) )
METHOD( SetRemoteElements, void, (void* self, int dim, int nEls, 
				  const int* globals), 
	(self, dim, nEls, globals) )
METHOD( AddRemoteElements, void, (void* self, int dim, int nEls, 
				  const int* globals), 
	(self, dim, nEls, globals) )
METHOD( RemoveRemoteElements, void, (void* self, int dim, int nEls, 
				     const int* globals, IMap* map), 
	(self, dim, nEls, globals, map) )
METHOD( SetIncidence, void, (void* self, int fromDim, int fromEl, 
			     int toDim, int nIncEls, const int* incEls ), 
	(self, fromDim, fromEl, toDim, nIncEls, incEls) )
METHOD( RemoveIncidence, void, (void* self, int fromDim, int toDim), 
	(self, fromDim, toDim) )
METHOD( InvertIncidence, void, (void* self, int fromDim, int toDim), 
	(self, fromDim, toDim) )
METHOD( ExpandIncidence, void, (void* self, int dim), (self, dim) )
METHOD( SetShadowDepth, void, (void* self, int depth), (self, depth) )
METHOD( Clear, void, (void* self), (self) )
METHOD( ClearDims, void, (void* self), (self) )
METHOD( ClearElements, void, (void* self), (self) )
METHOD( ClearIncidence, void, (void* self), (self) )

METHOD( GetNumDims, int, (const void* self), (self) )
METHOD( GetComm, const Comm*, (const void* self), (self) )
METHOD( HasDomain, Bool, (const void* self, int dim), (self, dim) )
METHOD( GetDomain, const Sync*, (const void* self, int dim), (self) )
METHOD( HasIncidence, Bool, (const void* self, int fromDim, 
			     int toDim), 
	(self, fromDim, toDim) )
METHOD( GetIncidenceSize, int, (const void* self, int fromDim, int fromEl, 
				int toDim), 
	(self, fromDim, fromEl, toDim) )
METHOD( GetIncidence, void, (const void* self, int fromDim, int fromEl, 
			     int toDim, int* nIncEls, const int** incEls), 
	(self, fromDim, fromEl, toDim, nIncEls, incEls) )

METHOD( PrintIncidence, void, (const void* self, int fromDim, int toDim), 
	(self, fromDim, toDim) )

MEMBER( int, nDims )
MEMBER( int, nTDims )
MEMBER( int, shadDepth )
MEMBER( Comm*, comm )
MEMBER( Decomp**, locals )
MEMBER( Sync**, remotes )
MEMBER( int***, nIncEls )
MEMBER( int****, incEls )
