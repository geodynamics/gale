<?xml version="1.0"?>
<!DOCTYPE StGermainData SYSTEM "stgermain.dtd">

<StGermainData xmlns="http://www.vpac.org/StGermain/XML_IO_Handler/Jun2003">

	<include> Underworld/REP_Setup.xml </include>

  <struct name="pluginData" mergeType="merge">
		<list name="NumericFields" mergeType="merge">
    <!-- the 2nd arg of these tuples corresponds to the ordering of analytic fields
    in the analytic plugin -->
      <!-- add the recovered fields to the things to test -->
			<param>recoveredSigmaField</param> <param>3</param>
			<param>recoveredPressureField</param> <param>1</param>
			<param>recoveredEpsDotField</param> <param>2</param>
		</list> 
	</struct>
</StGermainData>
